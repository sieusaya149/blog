name: Docker Image CI

on:
  push:
    branches: [ "testActionsGit" ]
  pull_request:
    branches: [ "testActionsGit" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag test:v1